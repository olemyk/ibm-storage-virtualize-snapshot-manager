version: '3.8'

services:
  # PostgreSQL Database
  postgres:
    image: docker.io/library/postgres:16-alpine
    container_name: snapshot-manager-db
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${DB_NAME:-snapshots}
      POSTGRES_USER: ${DB_USER:-snapshots}
      POSTGRES_PASSWORD: ${DB_PASSWORD:?DB_PASSWORD is required}
      PGDATA: /var/lib/postgresql/data/pgdata
    volumes:
      - postgres-data:/var/lib/postgresql/data
      - ./backend/scripts/postgres-init.sql:/docker-entrypoint-initdb.d/init.sql:ro
    networks:
      - snapshot-manager-net
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USER:-snapshots} -d ${DB_NAME:-snapshots}"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s
    security_opt:
      - no-new-privileges:true
    read_only: true
    tmpfs:
      - /tmp
      - /var/run/postgresql

  # Go Backend API (Pre-built from GHCR)
  backend:
    image: ghcr.io/olemyk/ibm-storage-virtualize-snapshot-manager/backend:latest
    container_name: snapshot-manager-backend
    restart: unless-stopped
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      # Server configuration
      SERVER_HOST: 0.0.0.0
      SERVER_PORT: 8080
      ALLOWED_ORIGINS: ${ALLOWED_ORIGINS}
      
      # Database configuration
      DB_TYPE: postgres
      DB_HOST: postgres
      DB_PORT: 5432
      DB_NAME: ${DB_NAME:-snapshots}
      DB_USER: ${DB_USER:-snapshots}
      DB_PASSWORD: ${DB_PASSWORD:?DB_PASSWORD is required}
      
      # Security configuration (REQUIRED)
      JWT_SECRET: ${JWT_SECRET:?JWT_SECRET is required}
      ENCRYPTION_KEY: ${ENCRYPTION_KEY:?ENCRYPTION_KEY is required}
      
      # Logging configuration
      LOG_LEVEL: ${LOG_LEVEL:-info}
      LOG_FILE: /app/logs/app.log
    volumes:
      - backend-logs:/app/logs
    networks:
      - snapshot-manager-net
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:8080/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    security_opt:
      - no-new-privileges:true
    read_only: true
    tmpfs:
      - /tmp

  # React Frontend with Nginx (Pre-built from GHCR)
  frontend:
    image: ghcr.io/olemyk/ibm-storage-virtualize-snapshot-manager/frontend:latest
    container_name: snapshot-manager-frontend
    restart: unless-stopped
    depends_on:
      backend:
        condition: service_healthy
    ports:
      - "${FRONTEND_HTTPS_PORT:-443}:443"
    volumes:
      # Mount custom SSL certificates if available
      - ${SSL_CERT_PATH:-./ssl}:/etc/nginx/ssl:ro
      # Mount custom Nginx config if needed
      # - ./frontend/custom-nginx.conf:/etc/nginx/nginx.conf:ro
    networks:
      - snapshot-manager-net
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:80/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
    security_opt:
      - no-new-privileges:true

networks:
  snapshot-manager-net:
    driver: bridge

volumes:
  postgres-data:
    driver: local
  backend-logs:
    driver: local
